/**
 * Package contains listeners and associated types which are used to track any 
 * runtime changes with the internal data structures defined in {@code pulse.input}.
 * This currently only includes a listener for {@code ExperimentalData}, since the 
 * latter can be truncated during runtime.
 */

package pulse.input.listeners;