/**
 * A package that is responsible for (a) plotting charts to display and interact with data from
 *  {@code HeatingCurve} objects; and (b) plotting preview charts from {@code ResultTable}s.
 */

package pulse.ui.charts;