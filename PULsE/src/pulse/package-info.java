/**
 * Contains some of the most frequently used classes, which did not seem to fit in any other packages.
 * Currently consists of {@code HeatingCurve} and {@code Baseline}. Both are extensively used throughout
 * other packages.
 */

package pulse;