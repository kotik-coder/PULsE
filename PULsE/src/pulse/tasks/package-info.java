/**
 * Introduces the {@code SearchTask}, which is the main actor in {@code PULsE}, and the {@code TaskManager}, a high-level
 * class which among other functions manages loading and executing {@code SearchTask}s. In addition, lists classes
 * for logging, storing runtime information including statuses, storing and ordering of final execution results,
 * storing intermediate results of execution to check convergence. 
 */

package pulse.tasks;