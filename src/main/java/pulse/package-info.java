/**
 * Contains some of the most frequently used classes, which did not seem to fit
 * in any other packages. Currently consists of {@code HeatingCurve} 
 * extensively used in other packages.
 */

package pulse;