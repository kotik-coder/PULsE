/**
 * Package contains listeners and event types which are used to track any
 * runtime changes with the internal data structures defined in
 * {@code pulse} and {@code pulse.input}.
 * @see pulse
 * @see pulse.input 
 */

package pulse.input.listeners;