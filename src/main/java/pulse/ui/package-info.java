/**
 * Includes the {@code Launcher} class, which is the entry-point to
 * {@code PULsE}. In addition, this package also includes the
 * 'message.properties' text file, which is used for storing verbose text
 * constants used e.g. by the GUI.
 */
package pulse.ui;
